import encounterFilter from './src/win-encounter-filter.vue';

encounterFilter.install = function(Vue) {
  // Vue.component(encounterFilter.name, encounterFilter);
  console.log('test');
};

export default encounterFilter;
